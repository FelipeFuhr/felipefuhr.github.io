# Contributing to felipefuhr.github.io

Before a PR: `make ci` (lint + test) must pass. See `AGENTS.md` for constraints.
New reusable-workflow references must stay classified in the ci-local registry
(this repo's CI runs the drift gate on itself).
